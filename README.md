Environment secrets access in jobs
Settings --> Environments --> dev and stage --> Created a secret name SEC (which having different values in both environments)
Accessing that secrets in workflow file.
